<div id="nav_wrap">
  <div id="logo">
    <div class="weather"></div>
  </div>
  <nav role='navigation'>
    <%= link_to "Back", id:"home" %><!--
    ---><a href="#" id="Things_todo">Things to do</a><!--
    ---><a href="#" id="Budget">Tasks</a><!--
    ---><a href="#" id="Wardrobe">Wardrobe</a>
  </nav>
  <div id="social">
    <div class="icon-facebook-sign"><%= link_to "Logout", logout_path, method: :delete %></div>
  </div><!--/social-->
</div><!--/nav_wrap-->

<section class="trips">
  <div id="copy">
  <div id="mainlogo">
    <span class="destination_input">
      <h1 class="name"><%= @trip.destination.capitalize %></h1>
    </span>
  <br />
    <div id="mainsocial">
      <div class="icon-facebook-sign"></div>
      <div class="icon-twitter-sign"></div>
    </div>
  </div>

    </div>
</section><!--/home-->

<section id="things_todo" class="bodysec">
  <div id="copy">
  <% if @popular_things_todo != nil  %>
    <div class="box-table">
      <table data-fn="contacts">
        <tbody>
          <% @popular_things_todo.each do |activity| %>
          <% if activity != nil%>
            <tr>
              <td class="Destination_image"><%= image_tag(activity["imageUrl"], size: "30x30")%></td>
              <td><%= activity["title"] %></td>
              <td><%= activity["fromPrice"] %></td>
              <td><%= activity["duration"] %></td>
              <td><span class="add"><%= link_to "√"%></span></td>
            </tr>
          <% end %>
        <% end %>
        </tbody>
        </table>
      </div>
      <% else %>
      <p>Unfortunately, no activities were found in <%= @trip.destination.capitalize %></p>
    <% end %>
  </div>
</section>

<section id="budget" class="bodysec">
<span class="hide">CLICK ME TO HIDE</span>

  <div id="copy">
    <span class='tasks'></span>
    <% if @tasks != nil %>
      <% @tasks.each do |task| %>
      <%= task.title %>
      <%= task.description %>
      <%= task.date %>
      <% end %>
    <% end %>

    <!-- <span class="see_tasks"><%= link_to "Show Tasks", "/" + current_user.user_name + "/" + @trip.id.to_s +  "/tasks" %></span> -->
  </div>

  <div class="box-table">
    <!-- <table data-fn="contacts"> -->
      <table class="calendar-table">
        <tr class="calendar-header"><span class="destination_input"><%= @trip.destination.capitalize %></span></tr>
        <tbody>
          <% @calendar.each do |date| %>
            <tr>
              <td class="calendar-header-day"><%= date %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    <!-- </table> -->
  </div>
  <br><br>

  <!-- <div class="form-group hide"> -->
    <div class="form-horizontal">
      <%= form_for(Task.new, url: "/" + current_user.user_name + "/" + @trip.id.to_s + "/tasks") do |f| %>
        <div class="form-group">
          <label class="col-xs-1 control-label">Task(s)</label>
          <div class="col-xs-3">
            <%= f.text_field :title, class: "form-control" %>
          </div>
          <div class="col-xs-3">
            <%= f.text_area :description, class: "form-control" %>
          </div>
          <div class="col-xs-4">
            <span class="glyphicon glyphicon-calendar"></span>
            <%= f.date_field :date, class: 'datepicker' %>
            <i class="fa fa-plus"><span class="addTask"><%= f.submit "+", class: "btn btn-default" %></span></i>
          </div>
        </div>
      <% end %>
    </div>
  <!-- </div> -->
</section>

<section id="wardrobe">
  <div id="copy">
    <h1>Wardrobe</h1>
    <p></p>
  </div>
</section>
